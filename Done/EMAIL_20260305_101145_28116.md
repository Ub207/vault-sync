---
type: email
from: "chatgpt-codex-connector[bot]" <notifications@github.com>
subject: Re: [openai/openai-agents-python] Add a quick opt-in option to switch

 to gpt-5 model (PR #1534)
received: 2026-03-05 10:11:45
date_header: Tue, 19 Aug 2025 18:26:42 -0700
priority: normal
status: awaiting_approval
uid: 28116
---

## Email Content

**From:** "chatgpt-codex-connector[bot]" <notifications@github.com>
**Subject:** Re: [openai/openai-agents-python] Add a quick opt-in option to switch

 to gpt-5 model (PR #1534)
**Date:** Tue, 19 Aug 2025 18:26:42 -0700

### Body (preview)

@chatgpt-codex-connector[bot] commented on this pull request.



Codex Review: Here are some suggestions.



Reply with `@codex fix comments` to fix any unresolved comments.



<details> <summary>About Codex in GitHub</summary>



[Your team has set up Codex to review pull requests in this repo](http://chatgpt.com/codex/settings/general). Reviews are triggered when you open a pull request that is ready for review, or mark a draft as ready for review. You can also ask for a review by commenting "@codex review".



Codex can also answer questions or update the PR. Try commenting "@codex fix this CI failure" or "@codex address that feedback".

</details>



> +        if (

+            # The user sets a non-default model

+            self.model is not None

+            and (

+                # The default model is gpt-5

+                is_gpt_5_default() is True

+                # However, the specified model is not a gpt-5 model

+                and (

+                    isinstance(self.model, Model)

+                    or gpt_5_reasoning_settings_required(self.model) is False

+                )

+                # The model settings are not customized for the specified model

+                and self.model_settings == get_default_model_settings()

+            )

+        ):

+            # In this scenario, we should use a generic model settings

+            # because non-gpt-5 models are not compatible with the default gpt-5 model settings.

+            # This is a best-effort attempt to make the agent work with non-gpt-5 models.



**[P1] Don't strip GPT-5 reasoning when a Model instance is supplied**



In `Agent.__post_init__` the guard at lines 294-307 treats any `Model` instance as a non–GPT‑5 model (`isinstance(self.model, Model)` short‑circuits). If `OPENAI_DEFAULT_MODEL` is GPT‑5 and the caller passes a GPT‑5 `Model` object, the code resets `self.model_settings` to `ModelSettings()`, removing the required reasoning config. GPT‑5 calls then f

---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
