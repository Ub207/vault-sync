---
type: email
from: roflov2 <notifications@github.com>
subject: Re: [openai/openai-agents-python] Local LLM implementation endlessly

 calling tool with no final answer (Issue #1544)
received: 2026-03-05 10:14:09
date_header: Thu, 21 Aug 2025 16:46:27 -0700
priority: normal
status: awaiting_approval
uid: 28342
---

## Email Content

**From:** roflov2 <notifications@github.com>
**Subject:** Re: [openai/openai-agents-python] Local LLM implementation endlessly

 calling tool with no final answer (Issue #1544)
**Date:** Thu, 21 Aug 2025 16:46:27 -0700

### Body (preview)

roflov2 left a comment (openai/openai-agents-python#1544)



Thanks for the quick response! I've tried all sorts of models like qwen3 and magistral with the same result. When it came to gpt-oss:20b, I got a different error: [gpt-oss.txt](https://github.com/user-attachments/files/21927525/gpt-oss.txt)



The variation in the code below, however, eliminated all issues.



```

import asyncio

import os

from openai import AsyncOpenAI

from agents import Agent, OpenAIChatCompletionsModel, Runner, function_tool, set_tracing_disabled



# Set the configuration for your local Ollama setup

BASE_URL = "http://localhost:11434/v1"

API_KEY = "ollama"

MODEL_NAME = "magistral:latest"



"""This example uses a custom provider for a specific agent. Steps:

1. Create a custom OpenAI client.

2. Create a `Model` that uses the custom client.

3. Set the `model` on the Agent.

Note that in this example, we disable tracing under the assumption that you don't have an API key

from platform.openai.com. If you do have one, you can either set the `OPENAI_API_KEY` env var

or call set_tracing_export_api_key() to set a tracing specific key.

"""



client = AsyncOpenAI(base_url=BASE_URL, api_key=API_KEY)

set_tracing_disabled(disabled=True)



# An alternate approach that would also work:

# PROVIDER = OpenAIProvider(openai_client=client)

# agent = Agent(..., model="some-custom-model")

# Runner.run(agent, ..., run_config=RunConfig(model_provider=PROVIDER))



@function_tool

def get_weather(city: str):

    print(f"[debug] getting weather for {city}")

    return f"The weather in {city} is sunny."



async def main():

    # This agent will use the custom LLM provider

    agent = Agent(

        name="Assistant",

        instructions="You only respond in haikus.",

        model=OpenAIChatCompletionsModel(model=MODEL_NAME, openai_client=client),

        tools=[get_weather],

    )

    

    result = await Runner.run(agent, "What's the weather in Tokyo?")

    print(result.final_outp

---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
