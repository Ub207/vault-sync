---
type: email
from: Kazuhiro Sera <notifications@github.com>
subject: Re: [openai/openai-agents-python] Feat: Add AdvancedSQLiteSession

 with conversation branching & usage tracking (PR #1662)
received: 2026-03-06 07:41:56
date_header: Mon, 22 Sep 2025 22:36:34 -0700
priority: normal
status: awaiting_approval
uid: 31103
---

## Email Content

**From:** Kazuhiro Sera <notifications@github.com>
**Subject:** Re: [openai/openai-agents-python] Feat: Add AdvancedSQLiteSession

 with conversation branching & usage tracking (PR #1662)
**Date:** Mon, 22 Sep 2025 22:36:34 -0700

### Body (preview)

@seratch requested changes on this pull request.



Great work! Left a few minor comments



> @@ -0,0 +1,111 @@

+"""



It's fine to have a long example code, so can you merge example_1 and example_2 into one file?



> +        if branch_id is None:

+            branch_id = self._current_branch_id

+

+        def _get_turns_sync():

+            conn = self._get_connection()

+            with closing(conn.cursor()) as cursor:

+                cursor.execute(

+                    """

+                    SELECT

+                        ms.branch_turn_number,

+                        am.message_data,

+                        ms.created_at

+                    FROM message_structure ms

+                    JOIN agent_messages am ON ms.message_id = am.id

+                    WHERE ms.session_id = ? AND ms.branch_id = ?

+                    AND ms.message_type = 'user'                    ORDER BY ms.branch_turn_number



Can you have a line break before ORDER BY here?



> +

+        turn_content = await asyncio.to_thread(_validate_turn)

+

+        # Generate branch name if not provided

+        if branch_name is None:

+            timestamp = int(time.time())

+            branch_name = f"branch_from_turn_{turn_number}_{timestamp}"

+

+        # Copy messages before the branch point to the new branch

+        await self._copy_messages_to_new_branch(branch_name, turn_number)

+

+        # Switch to new branch

+        old_branch = self._current_branch_id

+        self._current_branch_id = branch_name

+

+        self._logger.info(



Using debug-level logging for these ones is reasonable too (if a dev really wants to have an info-level log for this timing, they want to have more specific details as well). Thoughts?



> +        if branch_id is None:

+            branch_id = self._current_branch_id

+

+        def _search_sync():

+            conn = self._get_connection()

+            with closing(conn.cursor()) as cursor:

+

---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
