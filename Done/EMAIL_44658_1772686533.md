---
type: email
from: Aditya Singh <notifications@github.com>
subject: Re: [openai/openai-agents-python] feat: add

 ModelSettings.max_parallel_tool_calls for SDK-side concurrency limiting (PR

 #2599)
date: Wed, 04 Mar 2026 18:49:00 -0800
received: 2026-03-05 09:55:33
status: awaiting_approval
---

## Email Content
@adityasingh2400 commented on this pull request.







> @@ -1219,6 +1219,14 @@ def __init__(

         self.pending_tasks: set[asyncio.Task[Any]] = set()

         self.propagating_failure: BaseException | None = None

 

+        # Resolve max_parallel_tool_calls from the merged model settings.

+        # Agent-level settings are the base; run-level config settings override them.

+        resolved_settings = agent.model_settings.resolve(config.model_settings)

+        max_parallel = resolved_settings.max_parallel_tool_calls

+        self._concurrency_semaphore: asyncio.Semaphore | None = (

+            asyncio.Semaphore(max(1, max_parallel)) if max_parallel is not None else None



Fixed. Added a __post_init__ validator that raises ValueError for any value less than 1, with a clear
