---
type: email
from: Kotha Dhakshin <notifications@github.com>
subject: [openai/openai-agents-python] fix(retry): cap jittered delay to

 max_delay and catch OverflowError in Retry-After parsing (PR #2676)
received: 2026-03-15 22:05:43
date_header: Sun, 15 Mar 2026 07:54:25 -0700
priority: normal
status: awaiting_approval
uid: 45714
---

## Email Content

**From:** Kotha Dhakshin <notifications@github.com>
**Subject:** [openai/openai-agents-python] fix(retry): cap jittered delay to

 max_delay and catch OverflowError in Retry-After parsing (PR #2676)
**Date:** Sun, 15 Mar 2026 07:54:25 -0700

### Body (preview)

## Summary



This PR fixes two bugs introduced in the `feat: add opt-in model retry policies` commit (3a52673) inside `src/agents/run_internal/model_retry.py`.



---



### Bug 1: Jitter can produce delay &gt; `max_delay` (silent backoff violation)



**Location:** `_default_retry_delay()`, lines 287–290



**Root cause:**

The original code caps the exponential delay with `min(..., max_delay)` *before* applying jitter:

```python

# BEFORE (buggy)

delay = min(initial_delay * (multiplier ** max(attempt - 1, 0)), max_delay)

if not use_jitter:

    return delay

return max(delay * (0.875 + random.random() * 0.25), 0.0)

```

The jitter multiplier is sampled from `[0.875, 1.125]`. When the factor exceeds 1.0 (~50% of calls), the returned delay exceeds `max_delay`. For example with `max_delay=2.0` and jitter factor `1.1`, the actual sleep is `2.2 s` — silently violating the user-configured cap.



**Fix:** Move the `min(..., max_delay)` clamp to *after* the jitter multiplication:

```python

# AFTER (fixed)

delay = initial_delay * (multiplier ** max(attempt - 1, 0))

if not use_jitter:

    return min(delay, max_delay)

return min(max(delay * (0.875 + random.random() * 0.25), 0.0), max_delay)

```



---



### Bug 2: `OverflowError` not caught when parsing `Retry-After` header



**Location:** `_parse_retry_after()`, lines 97 and 108



**Root cause:**

The code calls `float(retry_after_ms)` / `float(retry_after)` inside `try/except ValueError` blocks. However, `float()` raises `OverflowError` (not `ValueError`) for strings representing very large numbers, e.g. `float(&quot;1e999&quot;)` → `OverflowError: (34, &#39;Result too large&#39;)`. A malicious or buggy server returning a header like `Retry-After: 1e999` would crash the retry loop with an unhandled exception instead of silently ignoring the bad header.



**Fix:** Catch both `ValueError` and `OverflowError`:

```python

except (ValueError, OverflowError):

    parsed_seconds = None

```



---



### Test c

---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
