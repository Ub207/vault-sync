---
type: email
from: Kazuhiro Sera <notifications@github.com>
subject: Re: [openai/openai-agents-python] Add SQLAlchemy session backend for

 conversation history management (PR #1357)
received: 2026-03-05 10:13:30
date_header: Thu, 21 Aug 2025 06:27:14 -0700
priority: normal
status: awaiting_approval
uid: 28281
---

## Email Content

**From:** Kazuhiro Sera <notifications@github.com>
**Subject:** Re: [openai/openai-agents-python] Add SQLAlchemy session backend for

 conversation history management (PR #1357)
**Date:** Thu, 21 Aug 2025 06:27:14 -0700

### Body (preview)

@seratch requested changes on this pull request.



can you check my last two requests?



> +                )

+                res = await sess.execute(subq)

+                row_id = res.scalar_one_or_none()

+                if row_id is None:

+                    return None

+                # Fetch data before deleting

+                res_data = await sess.execute(

+                    select(self._messages.c.message_data).where(self._messages.c.id == row_id)

+                )

+                row = res_data.scalar_one_or_none()

+                await sess.execute(delete(self._messages).where(self._messages.c.id == row_id))

+

+                if row is None:

+                    return None

+                try:

+                    return json.loads(row)  # type: ignore[no-any-return]



_deserialize as well? also make the internal methods async not to receive such a breaking change request in the future



> @@ -164,6 +164,64 @@ result2 = await Runner.run(

 )

 ```

 

+### SQLAlchemy-powered sessions



can you move this document change to a separate pull request? with that, we can merge this PR immediately and then we can merge the document changes after the release



-- 

Reply to this email directly or view it on GitHub:

https://github.com/openai/openai-agents-python/pull/1357#pullrequestreview-3140681387

You are receiving this because you are subscribed to this thread.



Message ID: <openai/openai-agents-python/pull/1357/review/3140681387@github.com>

---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
