---
type: email
from: Christian Ress <notifications@github.com>
subject: [openai/openai-agents-python] OpenAIResponsesCompactionSession + reasoning breaks with gpt-5.4 (compact output retains orphaned assistant message IDs) (Issue #2727)
received: 2026-03-20 07:05:49
date_header: Thu, 19 Mar 2026 18:10:31 -0700
priority: normal
status: pending
uid: 19d08cb40252ad6e
---

## Email Content

**From:** Christian Ress <notifications@github.com>
**Subject:** [openai/openai-agents-python] OpenAIResponsesCompactionSession + reasoning breaks with gpt-5.4 (compact output retains orphaned assistant message IDs) (Issue #2727)
**Date:** Thu, 19 Mar 2026 18:10:31 -0700

### Body (preview)

ress created an issue (openai/openai-agents-python#2727)



### Describe the bug



`responses.compact` with **gpt-5.4** strips reasoning items from its output but retains assistant messages with their original IDs. When the compacted output is sent back to `responses.create`, the API rejects it because each assistant message ID implies a required paired reasoning item that was removed during compaction.



**This is gpt-5.4 specific.** The same workflow with gpt-5.2 works fine — gpt-5.2's `responses.compact` does not retain assistant message IDs in its output (0 assistant messages with IDs vs 3 for gpt-5.4).



This means **any conversation using reasoning + compaction with gpt-5.4 is permanently broken after the first compaction**.



`reasoning_item_id_policy="omit"` does not help because it strips IDs from reasoning items, not from the assistant messages that reference them.



### Debug information

- Agents SDK version: v0.12.5 (also reproduced on v0.10.5)

- Python version: Python 3.13.5

- OpenAI SDK version: 2.29.0

- Model: gpt-5.4 (gpt-5.2 works correctly)



### Repro steps



#### Minimal repro (adapts SDK's own `examples/memory/compaction_session_example.py`)



With **gpt-5.4** — fails at turn 5 (first turn after compaction):



```python

import asyncio



from agents import Agent, ModelSettings, OpenAIResponsesCompactionSession, Runner, SQLiteSession

from agents.model_settings import Reasoning





async def main():

    underlying = SQLiteSession(":memory:")

    session = OpenAIResponsesCompactionSession(

        session_id="demo-session",

        underlying_session=underlying,

        model="gpt-5.4",

        should_trigger_compaction=lambda ctx: len(ctx["compaction_candidate_items"]) >= 4,

    )



    agent = Agent(

        name="Assistant",

        instructions="Reply concisely. Keep answers to 1-2 sentences.",

        model="gpt-5.4",

        model_settings=ModelSettings(reasoning=Reasoning(effort="low")),

    )



    prompts = [


---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
