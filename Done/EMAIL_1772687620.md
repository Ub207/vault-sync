---
type: email
from: Kazuhiro Sera <notifications@github.com>
subject: Re: [openai/openai-agents-python] fix: isolate parallel function tool

 failures (PR #2584)
received: 2026-03-05 10:13:40
priority: high
status: awaiting_approval
---

## Email Content
@seratch commented on this pull request.







> +                tool_run = task_to_tool_run[task]

+                try:

+                    results_by_tool_run[id(tool_run)] = task.result()

+                except BaseException as exc:

+                    # A single wait batch can contain multiple finished tasks, so preserve

+                    # fatal control-flow exceptions over ordinary tool errors.

+                    if isinstance(exc, Exception):

+                        if non_fatal_failure is None:

+                            non_fatal_failure = exc

+                    elif fatal_failure is None:

+                        fatal_failure = exc

+            failure = fatal_failure or non_fatal_failure

+            if failure is not None:

+                failure =...

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Flag for review
