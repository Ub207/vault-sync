---
type: email
from: Hassan Abu Alhaj <notifications@github.com>
subject: Re: [openai/openai-agents-python] Feat: Add AdvancedSQLiteSession

 with conversation branching & usage tracking (PR #1662)
received: 2026-03-06 07:42:04
date_header: Tue, 23 Sep 2025 03:08:37 -0700
priority: normal
status: awaiting_approval
uid: 31117
---

## Email Content

**From:** Hassan Abu Alhaj <notifications@github.com>
**Subject:** Re: [openai/openai-agents-python] Feat: Add AdvancedSQLiteSession

 with conversation branching & usage tracking (PR #1662)
**Date:** Tue, 23 Sep 2025 03:08:37 -0700

### Body (preview)

@habema commented on this pull request.







> +    def _is_user_message(self, item: TResponseInputItem) -> bool:

+        """Check if this is a user message."""

+        return isinstance(item, dict) and item.get("role") == "user"

+

+    async def get_items(

+        self, limit: int | None = None, include_inactive: bool = False

+    ) -> list[TResponseInputItem]:

+        """Get items, optionally including soft-deleted ones."""

+        if include_inactive:

+            # Use parent implementation for all items

+            return await super().get_items(limit)

+

+        # Filter to only active items

+        def _get_active_items_sync():

+            conn = self._get_connection()

+            with self._lock if self._is_memory_db else threading.Lock():



Sure thing



> +

+        turn_content = await asyncio.to_thread(_validate_turn)

+

+        # Generate branch name if not provided

+        if branch_name is None:

+            timestamp = int(time.time())

+            branch_name = f"branch_from_turn_{turn_number}_{timestamp}"

+

+        # Copy messages before the branch point to the new branch

+        await self._copy_messages_to_new_branch(branch_name, turn_number)

+

+        # Switch to new branch

+        old_branch = self._current_branch_id

+        self._current_branch_id = branch_name

+

+        self._logger.info(



I think debug is more suitable yes



-- 

Reply to this email directly or view it on GitHub:

https://github.com/openai/openai-agents-python/pull/1662#discussion_r2371798888

You are receiving this because you are subscribed to this thread.



Message ID: <openai/openai-agents-python/pull/1662/review/3257189028@github.com>

---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
