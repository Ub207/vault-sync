---
type: email
from: Harish Raju <notifications@github.com>
subject: Re: [openai/openai-agents-python] How do I handle image outputs from

 tools and feed it back to the LLM during an agent run? (Issue #1553)
received: 2026-03-05 10:14:23
date_header: Thu, 21 Aug 2025 23:27:24 -0700
priority: normal
status: awaiting_approval
uid: 28366
---

## Email Content

**From:** Harish Raju <notifications@github.com>
**Subject:** Re: [openai/openai-agents-python] How do I handle image outputs from

 tools and feed it back to the LLM during an agent run? (Issue #1553)
**Date:** Thu, 21 Aug 2025 23:27:24 -0700

### Body (preview)

probably-harish left a comment (openai/openai-agents-python#1553)



example:



```

# to test image output from tool calls for agents



from agents import Agent, Runner, function_tool

import base64

import asyncio





IMAGE_PATH = 'data/sample.png'



@function_tool

def return_image_base64() -> str:

    with open(IMAGE_PATH, "rb") as image_file:

        # return f"data:image/png;base64,{base64.b64encode(image_file.read()).decode('utf-8')}"

        # return {

        #     "data_type": "data:image/png;base64",

        #     "data": base64.b64encode(image_file.read()).decode('utf-8')

        # }

        return "sample base64 string"





async def main():

    agent = Agent(

        name="Agent with image tools",

        instructions="""

        Mode 1: return "using mode 1; input received: {input}"

        Do NOT use the tool to return the image.



        Mode 2:

        You are an agent that I am using to test image tools. Use the tool `return_image_base64` to return a base64 encoded image. You can use the tool as many times as you want.

        You are to ALWAYS use the tool to return the image, even if the user request is not related to the image.

        The tool will return a base64 encoded image, as 

        return {

            "data_type": "data:image/png;base64",

            "data": base64.b64encode(image_file.read()).decode('utf-8')

        }

        NOTE: the return value from the tool should be sent not as string but as a dict with the key "data" and the value being the base64 encoded image.

        The subsequest request after the tool call should be in the following format and NOT as a string:

            input=[

            {

                "role": "user",

                "content": [

                    {"type": "input_text", "text": "describe the image in detail"},

                    {"type": "input_image", "image_url":  f"data:image/png;base64,{image_base64}"}

                ]

            }

        ]

---

## Suggested Actions
- [ ] Reply
- [ ] Archive
- [ ] Escalate
