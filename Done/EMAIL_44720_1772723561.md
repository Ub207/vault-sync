---
type: email
from: Damian ONeill <notifications@github.com>
subject: [openai/openai-agents-python] feat: add MCP tool title support to

 ToolCallItem (PR #2604)
date: Thu, 05 Mar 2026 06:32:23 -0800
received: 2026-03-05 20:12:41
status: awaiting_approval
---

## Email Content
Closes #2603



- Add `title: str | None` field to `ToolCallItem` (items.py) to surface the short human-readable display label separately from the long-form description. For MCP tools, resolved from tool.title then tool.annotations.title.



- Update `MCPUtil.to_function_tool()` (mcp/util.py) to import `get_display_name` from `mcp.shared.metadata_utils` and use it as a fallback when `tool.description` is absent or empty, so the model always receives a non-empty description string (title &gt; annotations.title &gt; name).



- Populate both `description` and `title` on `ToolCallItem` in `process_model_response` (turn_resolution.py) from the server&#39;s cached MCP tool metadata.



- Build a precomputed `mcp_tool_map` in `run_single_turn_streamed` (run_loop.py) and populate both fields on t
